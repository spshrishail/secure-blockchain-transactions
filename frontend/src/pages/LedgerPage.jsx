import { Box } from '@mui/material';
import Ledger from '../components/Ledger';

const LedgerPage = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Ledger />
    </Box>
  );
};

export default LedgerPage;